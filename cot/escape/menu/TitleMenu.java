package cot.escape.menu;

import cot.escape.*;

public class TitleMenu extends Menu {
  public void tick(Game game, boolean up, boolean down, boolean left,
                   boolean right, boolean use) {
    System.out.println("In TitleMenu.tick");
  }
}
